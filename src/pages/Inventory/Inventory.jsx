// import { useLogOutRedirect } from "hooks";

const Inventory = () => {
    // useLogOutRedirect();
    return (
        <>
            <h1>Welcome to inventory page</h1>
        </>
    );
};

export default Inventory;
